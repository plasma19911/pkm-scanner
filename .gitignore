# Caches - gehören nicht ins Repository.
# shop_pages_cache.json ist bei dir rund 140 MB groß; GitHub lehnt einzelne
# Dateien über 100 MB komplett ab und warnt schon ab 50 MB.
shop_pages_cache.json
release_calendar_cache.json

# Nur die Arbeitskopien im Hauptverzeichnis ausschließen - der führende
# Schrägstrich ist wichtig! Ohne ihn gilt das Muster in JEDEM Ordner und
# würde auch docs/data.json verschlucken, also genau die Datei, aus der
# die App ihre Angebote liest.
/angebote.html
/data.json

# Windows-Beiwerk
Thumbs.db
desktop.ini
__pycache__/
